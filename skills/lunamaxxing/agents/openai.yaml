interface:
  display_name: "LunaMaxxing"
  short_description: "Native Luna Max orchestration with bounded subagents"
  default_prompt: "Use $lunamaxxing to solve this task with the DIRECT, DELEGATED, or FANOUT route, keeping the main session in control."
policy:
  allow_implicit_invocation: false
